version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Set GOOGLE_APPLICATION_CREDENTIALS
          run: |
            echo "$GCP_SA_JSON_B64" | base64 -d > /tmp/gcp-sa.json
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp-sa.json" >> $ENV0_ENV
